{ 
  "name": "base-ds-container",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "settings": {
    "terminal.integrated.defaultProfile.linux": "bash"
  },
  "extensions": [
    "ms-python.python",
    "ms-toolsai.jupyter",
    "ms-azuretools.vscode-docker"
  ],
  "postCreateCommand": "pip install -r .devcontainer/requirements.txt && python -m ipykernel install --user --name devcontainer-kernel --display-name 'Python (DevContainer)'"
}
